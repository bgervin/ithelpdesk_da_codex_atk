# Teams App ID (automatically generated on provision)
# TEAMS_APP_ID=

# M365 App Name Suffix (optional, for dev environments)
# APP_NAME_SUFFIX=

# ServiceNow Instance Configuration
# SERVICENOW_INSTANCE=dev12345
# Note: Use instance name only (without .service-now.com)

# ServiceNow OAuth Configuration
# These values must be configured in M365 Plugin Vault
# SERVICENOW_OAUTH_CONFIG_ID=servicenow-oauth-prod
# SERVICENOW_CLIENT_ID=<from-servicenow-oauth-app>
# SERVICENOW_CLIENT_SECRET=<from-servicenow-oauth-app>

# OAuth URLs (automatically constructed from instance name)
# Authorization URL: https://${SERVICENOW_INSTANCE}.service-now.com/oauth_auth.do
# Token URL: https://${SERVICENOW_INSTANCE}.service-now.com/oauth_token.do
# Scopes: useraccount
