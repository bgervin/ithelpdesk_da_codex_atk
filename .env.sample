# M365 Teams App Configuration
TEAMS_APP_ID=<your-teams-app-id>

# ServiceNow Instance Configuration
SERVICENOW_INSTANCE=<your-instance-name>
# Example: dev12345 (without .service-now.com)

# ServiceNow OAuth Configuration
SERVICENOW_OAUTH_CONFIG_ID=<your-oauth-config-id>
SERVICENOW_CLIENT_ID=<your-client-id>
SERVICENOW_CLIENT_SECRET=<your-client-secret>

# OAuth URLs (automatically constructed from instance name)
# Authorization URL: https://${SERVICENOW_INSTANCE}.service-now.com/oauth_auth.do
# Token URL: https://${SERVICENOW_INSTANCE}.service-now.com/oauth_token.do
# Scopes: useraccount
