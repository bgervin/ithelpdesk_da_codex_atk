# Prism mock base
API_HOST=localhost
API_PORT=4010

# Declarative agent plugin will read this spec (relative path from appPackage/)
OPENAPI_URL=apiSpecificationFile/openapi.local.yaml

# ServiceNow OAuth Configuration
# Register your OAuth application in ServiceNow first:
# 1. Go to System OAuth → Application Registry
# 2. Create a new OAuth API endpoint for external clients
# 3. Copy the Client ID and Client Secret here

CLIENT_ID=your-servicenow-client-id
CLIENT_SECRET=your-servicenow-client-secret

# ServiceNow Instance Configuration
# Replace 'dev12345' with your actual ServiceNow instance name
SERVICENOW_INSTANCE=dev12345
SERVICENOW_BASE_URL=https://${SERVICENOW_INSTANCE}.service-now.com

# ServiceNow production (set your instance name in CI/CD or .env.prod)
SERVICENOW_BASE_URL_PROD=https://${SERVICENOW_INSTANCE}.service-now.com

# Microsoft 365 Configuration
# Get these from your Azure AD app registration
TENANT_ID=your-microsoft-tenant-id

# OAuth Registration ID (generated during M365 setup)
OAUTH2_REGISTRATION_ID=your-oauth-registration-id

# Teams App Configuration (generated during deployment)
TEAMS_APP_ID=
